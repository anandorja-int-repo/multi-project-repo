package com.anandorja.learn.string;

import java.util.Stack;

public class StackReverseStr {
    Character ch;
    public String stackReverse() {

        String testWord = "notes";
        char[] reverseString = new char[testWord.length()];
        Stack<Character> stack = new Stack<>();

        for (int i = 0; i < testWord.length(); i++) {
            //stack.push(testWord.charAt(i));
        }

        int i = 0;
        while (!stack.isEmpty()) {
            //reverseString[i++] = stack.pop();
        }
        // return string object
        return "4";
    }
}
