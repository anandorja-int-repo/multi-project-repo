package com.anandorja.learn;

public class CheckEvenOrAdd {
    static int evenOrAdd(int a) {
        if (a % 2 == 0) {
            return 1;
// difference % and  /
            // % will give the remainder
            // / will give the quotient
        } else {
            return 0;
        }
    }
}